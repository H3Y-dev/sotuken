# T1-3 イテレーション記録（Codexへの引き継ぎ資料）

設計方針の本体は `tasks/design/T1-3-目盛り検出の再設計.md`。
このファイルは**これまで何を試して、何が効いて何が効かなかったか**の記録。
同じ失敗を繰り返さないために必ず読むこと。

## 【2026-08-25 到達点】企業提供の実機写真4枚はすべてJIS 2.5級を満たした

**卒論の実験結果として最も重要な数字。**

| 企業提供写真（日本製鋼所様） | 引用誤差 |
|---|---|
| 気密試験_昇圧前圧力計 | **0.11 %FS** |
| 耐圧試験_昇圧後圧力計 | **0.47 %FS** |
| 耐圧試験_昇圧前圧力計 | **0.77 %FS** |
| 気密試験_昇圧後圧力計 | **1.99 %FS** |

**4枚すべてがJIS C 1102 2.5級の許容値（±2.5%FS）に収まっている。**
うち1枚（耐圧試験_昇圧後）はモニター画面越しの撮影で映り込み・グレアが
あるにもかかわらず0.47%FSで読めている。

### 対象別の成績（`--no-vlm`、決定論的条件）

| 対象 | 件数 | 平均 | 中央値 | 許容内 | 25%FS超 |
|---|---|---|---|---|---|
| **工場の実機メーター** | 13 | 12.19 | **2.84 %FS** | 6件 | 2件 |
| ストック画像（meter1〜4） | 4 | 18.04 | 3.50 %FS | 2件 | 1件 |

**最大の失敗 `meter3.png`（63.75%FS）はストック画像**（車載タコメーター）であり、
**納品対象ではない**。可視化して確認したところ、この1枚は
(a) 中心が針の回転軸からずれる、(b) ゼロ点・フル点が目盛り上に無い、
(c) 検出された針が**隣に写っている水温計の線**、と複数段階で破綻していた。
1枚の画像に複数のメーターが写っているためで、これはT4-1（複数メーター検出）の
領域。VLMによる盤面クロップは効いている（`cropped=True`）が結果は変わらなかった。

**評価の母数にストック画像を含めると実力より悪く見える。** 卒論に載せる際は
「工場の実機メーター」と「ストック画像」を分けて示すこと。

---

## 【2026-08-25追記3】試して効かなかった対策（繰り返さないこと）

以下はいずれも筋が通って見えるが、**決定論的A/B（`--no-vlm`）で純損失**だった。
同じ発想で作り直さないこと。

### 1. 暗背景メーターの明暗反転（2案とも不採用）

暗背景に白目盛りの計器（扇形メーター・タコメーター）が破滅的失敗の上位を
占めていたため、盤面輝度を測って暗ければ画像を反転する対策を試した。
盤面輝度は暗い側50〜79、明るい側186〜248とよく分離しており、判定自体は容易。

| 案 | 平均 | 中央値 | 除外平均 |
|---|---|---|---|
| なし | **20.59** | **7.00** | **5.69** |
| 画像全体を反転 | 109.01 | 9.00 | 7.14 |
| 目盛り検出時のみ反転 | 21.01 | 9.00 | 6.35 |

**扇形メーターの片方（134728）は31.47→2.95と劇的に改善するのに、
もう片方（134730）が29.10→1540.76と破綻する。** 全体反転はOCR側を壊し、
目盛り検出だけの反転では134728の改善効果（OCR由来だった）が消える。

→ 扇形メーターは単純な反転では扱えない。`tasks/todo.md` のT5-1/T5-2
（明暗の自動判定＋**色情報による指示針と設定針の区別**）として、
本来の設計で対処すること。

### 2. 確度階級表記（`2.5`）の除外（不採用）

`134614` / `134622` / `134606` の3枚で、スケール最小値が真値0ではなく
`2.5` と誤検出されていた。原因はOCRが盤面の
「JIS C1102 / PSK-100 / **2.5**」という**確度階級の印字**を目盛りの数字と
誤認していたこと（生テキストで確認済み。`2.5`は`PSK-100`とy座標1px差）。

`JIS` を含むテキストとの並記を条件に除外したところ、**3枚とも悪化した。**

| 画像 | 除外前 | 除外後 |
|---|---|---|
| 134606 | 2.84 | 72.00 |
| 134614 | 58.44 | 62.00 |
| 134622 | 18.23 | 59.29 |

**理由: 真の最小値`0`のラベルがそもそも検出できておらず、誤読の`2.5`が
偶然「0に近い最小値」の代役を果たしていた。** `2.5`を消しても`0`は
現れないため、より悪い最小値（10や15）が採用される。

→ **真の問題は「余計な数字がある」ことではなく「`0`が読めていない」こと。**
対策するなら、除外ではなく`0`の位置を補完する方向（`locate_value_by_extrapolation`
が既にある）を検討すること。

### 距離による識別が効かない理由（記録）

`2.5`と正当な目盛り数字は、次のどちらでも分離できなかった。

- **中心からの半径**: `2.5`は目盛り帯比0.97、正当な`10.0`は0.96
- **非数字テキストへの近さ**: 圧力計では正当な数字も`MPa`や`USE NO OIL`から
  横2.5〜3.7行分の位置にあり、`2.5`（`PSK-100`から約3.8行分）と重なる

---

## 【最重要・2026-08-25追記2】`--no-vlm` での評価は実運用と大きく違う

**これまでの測定はすべて `--no-vlm` で行っていたが、これは実運用の姿ではない。**
目盛り読み取りの信頼度が低い場合、パイプラインはVLMフォールバックに切り替わる。
`--no-vlm` はその救済を無効にした状態なので、**実力より悲観的な数字が出る。**

VLM有効（実運用相当）で測り直した結果、さらにフルスケールの妥当性チェックを
入れた後の到達点は次の通り。

| 指標 | no-vlm | VLM有効 | **VLM有効＋標準数チェック** |
|---|---|---|---|
| 許容誤差内 | 6/18 | 7/18 | **8/18** |
| 平均引用誤差 | 16.23 | 60.78 | **11.95 %FS** |
| **中央値** | 5.47 | 3.44 | **2.19 %FS** |
| 破滅的失敗 | 5 | 6 | **4件** |
| 読み取り成功 | 17/18 | 18/18 | 18/18 |

**中央値2.19%FSは、JIS C 1102 2.5級の許容値（±2.5%FS）を下回っている。**
つまり典型的なケースでは既に計器の確度階級を満たす精度が出ている。

### 単一画像が平均を破壊していた

`20260817_134619.jpg` でスケールが `20〜1111` と誤検出され（OCRが目盛り線を
数字と誤読）、真値45に対し1101.91、引用誤差**880%FS**を記録していた。
**この1枚だけで全体の平均引用誤差が16%FSから60%FSへ跳ね上がっていた。**

対策として、計器のフルスケールが標準数（1/1.5/2/2.5/3/4/5/6/7.5/8 に
10のべき乗）から選ばれる性質で妥当性を判定し、外れていれば値を捨てず
**信頼度を下げてVLMフォールバックに委ねる**ようにした（`595923f`）。
結果 880.76% → 11.01%。

### 今後の測定の作法

- **VLM有効で測ること。** `--no-vlm` は目盛り検出単体の比較にのみ使う
- 平均だけでなく**中央値と破滅的失敗の件数**を見る
- 1枚の異常値が平均を支配していないか毎回確認する

---

## 【重要・2026-08-25追記】判定基準を見直した

**「平均引用誤差を下げる」だけを目標にするのをやめる。** 実測で次が分かった。

| 統計 | 値（iter1時点） |
|---|---|
| 平均引用誤差 | 16.55 %FS |
| **中央値** | **5.47 %FS** |
| 25%FS超の破滅的失敗 | **5件で平均の79%を占める** |
| それ以外12件の平均 | **4.87 %FS**（許容値±2.5%にかなり近い） |

つまり**大半の画像は既にそこそこ読めており、少数の破綻が平均を吊り上げている。**
平均だけを追うと、12件の地道な改善が数値に現れない一方で、
**担当範囲外の失敗に振り回される。**

破滅的失敗5件のうち上位2件（`20260817_134728` 32.6%、`20260817_134730` 29.1%）は
**扇形メーター**で、暗背景に白目盛り・赤い設定針という別問題。
`tasks/todo.md`のT5-1/T5-2として切り出されている範囲であり、
**T1-3（目盛り検出）をいくら磨いても原理的に解決しない。**

### 検証済み: 「目盛り範囲(min/max)の誤検出が主因」は誤りだった

以前この記録に書いた仮説は、実験して**否定された**。
検出したスケール範囲を**真値に置き換えて**再計算しても:

- 平均引用誤差 16.55% → **15.80%FS**（0.75ポイントしか改善しない）
- 許容誤差内は 5件 → **4件に減る**

**ここを直しても報われない。** 目盛り範囲の改善に工数を割かないこと。

### 新しい判定基準

`evaluate.py` が中央値・破滅的失敗件数・それを除いた平均を出力するようにした。

- **主指標: 中央値引用誤差（現在 5.47 %FS）と、破滅的失敗を除いた平均（現在 4.87 %FS）**
- 平均引用誤差は参考値として見る（外れ値に支配されるため）
- **破滅的失敗の件数（現在5件）が減れば大きな前進**だが、うち2件は
  T5（扇形メーター）の担当範囲なのでT1-3では触らない

---

## 判定基準（旧・平均のみ。上記の見直し前の記録として残す）

```
venv\Scripts\python.exe evaluate.py eval/groundtruth.json --no-vlm -o eval/report_iterN.json
```

| 指標 | ベースライン | **iter1（採用中）** | iter2（退行・不採用） |
|---|---|---|---|
| 許容誤差内（±2.5%FS） | 2/18 | **5/18** | 5/18 |
| **平均引用誤差** | 21.67 %FS | **16.55 %FS** | 20.65 %FS |
| 読み取り成功 | 16/18 | 17/18 | 17/18 |
| 回帰テスト | 26件 | 28件 | 29件 |

**主指標は「平均引用誤差」。** 現在の到達点は `16.55 %FS`。
**これを下回れなければ採用しない。** テストが通っただけでは合格ではない。

## 元の実装が壊れていた理由（iter1で解決済み）

輪郭PCA方式だった頃、実写真で**真陽性がゼロ**だった。

- 隣接する目盛りが `adaptiveThreshold(blockSize=25)` で融合し、
  長さ508px・太さ108pxの巨大な連結成分になっていた
- 融合した塊は主軸（PCA第1主成分）が**円周方向**を向く
- 「主軸が中心方向を向いていること（差18度以内）」という条件で
  **本物の目盛りが全部棄却**されていた
- 一方、背景（壁・配管）の孤立した小片が偶然放射方向を向き、
  目盛りとして採用されていた

## iter1: 極座標変換への移行（成功・採用中）

コミット `c0e66d6 極座標変換で目盛り検出を再設計`。

- 中心周りに極座標変換し、角度方向の周期的な暗線を目盛りとして採用
- 盤面半径Rが得られる場合は `0.55R〜0.98R` に探索を限定
- **効果: 平均引用誤差 21.67 → 16.55 %FS、許容内 2 → 5件**
- **背景（壁・配管）の誤検出は完全に消えた。** 検出点は全て盤面内に入った

### iter1に残っている問題（目視確認で判明）

検出点が**目盛り帯ではなく「数字の輪」に乗っている**。
`気密試験_昇圧前圧力計.jpg` を目視した結果:

- 検出点が数字（0 / 50 / 100 / 150）・「PRESSURE GAUGE」の文字・
  「MPa」・下部の検定済シールの上に乗っている
- 本来の目盛り帯はもっと外側（ベゼルのすぐ内側）にある
- **原因: 数字も円周上に等間隔に並ぶため周期性を持つ。**
  「周期性が強い半径帯を選ぶ」だけでは目盛り帯と数字の輪を区別できない

## iter2: スコアリングによる帯選択（退行・不採用）

**やったこと:** 周期性だけでなく、周期格子の充足率・推定本数・
放射方向の細長さ・線長・**弱い外側優先（最大0.35点）** を加えた
スコアで半径帯を選ぶよう変更。

**結果: 平均引用誤差 16.55 → 20.65 %FS と悪化した。**
（許容内・読み取り成功数は変わらず。回帰テストは29件通っていた）

**重要: テストが全部通っても、評価基盤が悪化すれば不採用。**
iter2はまさにこのケースだった。

### iter2の半径測定値（参考。目標は 0.38〜0.42）

| 画像 | iter1 r_med/sh | iter2 r_med/sh | iter1 CV | iter2 CV |
|---|---|---|---|---|
| company0（気密・昇圧前） | 0.300 | 0.322 | 0.076 | 0.078 |
| company1（気密・昇圧後） | 0.281 | **0.370** | 0.058 | 0.045 |
| company2（耐圧・昇圧前） | 0.294 | 0.296 | 0.042 | 0.041 |
| company3（耐圧・昇圧後） | 0.316 | 0.316 | 0.207 | 0.207 |
| meter1.png | 0.344 | 0.344 | 0.135 | 0.135 |
| meter2.jpg | 0.345 | 0.364 | 0.077 | 0.123 |
| meter3.png | 0.242 | **0.308** | 0.150 | **0.072** |

半径が目標方向へ動いた画像（company1, meter3）はあったが、
**全体の読み取り精度はむしろ下がった。** つまり
「半径を外側へ寄せる」こと自体は目的ではない。目的は読み取り精度。

iter2の実装は `git stash` に退避してある（`stash@{0}: iter2-regressed`）。
参考にしたい場合は `git stash show -p stash@{0}` で差分を見られる。
**ただし戻して使うことはしない。**

## iter3への示唆

- **「外側を優先する」という単純な方向付けは効かなかった。** 別の切り口が要る
- 数字と目盛りを分ける手がかりの候補:
  - **本数（周波数）** — 目盛りは数十本、数字は4〜6個。桁違いに違う
  - **形状** — 目盛りは半径方向に細長い線、数字は縦横比が1に近い塊
  - iter2はこの両方をスコアに入れたが、他の項目（外側優先など）と
    合算したことで効果が薄まった可能性がある。**単独で効くかを個別に検証する**価値がある
- **company3（耐圧・昇圧後）はCV=0.207と突出して悪い。** この画像は
  モニター画面越しの撮影で映り込み・グレアがあり、条件が他と大きく違う。
  この1枚に引きずられて全体の設計を歪めないよう注意する
- **目盛り範囲（min/max）の誤検出が9/17残っている。** これは目盛り検出
  そのものより後段（OCRとの対応付け）の問題の可能性がある。
  平均引用誤差が下がりきらない主因はこちらかもしれない
