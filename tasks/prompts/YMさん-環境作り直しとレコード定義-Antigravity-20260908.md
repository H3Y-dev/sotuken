# このファイルの使い方

1. これはYMさんが **Antigravity CLI（`agy`）** で作業するための指示です。
2. **先に `tasks/Antigravity使い方マニュアル.md` の設定を済ませてください**（モデル: Gemini 3.8 Flash (Low) / Tool Permission: `always-proceed` / Verbosity: `high` / `GEMINI.md`）。
3. PowerShellで `cd C:\卒研\git_stk\sotuken` してから `agy` を起動し、下の区切り線より下を**全部コピー**して最初のメッセージとして貼り付ける。

> [!important] 2026-09-03に渡した指示と、2026-09-02に渡したYM-02の指示を1本に統合したものです
> 前半（環境の作り直し）が未着手のままなので、同じ内容を含んでいます。
>
> **次のどれかに当てはまったら、そこで手を止めて大久保へ連絡してください。**
>
> - 1-5 のテストが **90件にならない**（51件のままなど）
> - `git push` で `stale info` または `rejected` と出た
> - 手順に書いていないエラーが出た
> - Antigravityが、この指示書に無いファイルを変更しようとした

---

あなた（Antigravity CLI）へ。YMさんの担当タスクを、完了まで伴走してください。

## 0. 前提と、あなたの動き方

日本製鋼所様の工場にある丸型アナログ計器をAndroidで撮影し、PC側の既存Python（`meter_pipeline.py`）で読み取り、CSV/JSON Linesへ記録・集約する卒業研究です。11月頃に、撮影から集約までを通しでデモします。2026-09-01に読み取り精度は凍結し、いまは撮影から集約までを通しで動かす方針です。

5人チームで、本人（大久保）はAndroidキューと全体レビュー、SKくんはAndroidのUI、SRくんはPC側取り込み、YMさんは**共通レコードと保存規約**、KPくんは出力・一覧・境界を担当します。

**あなたはこのリポジトリのファイルを直接読み書きでき、コマンドも実行できます。** YMさんに中身を貼り付けてもらう必要はありません。ただし次を守ってください。

- **1回の返答で示すのは「次の一歩」だけ。** 手順1〜3を一気に進めないでください
- **コマンドを実行する前に、それが何をするコマンドかを日本語で1〜2行説明する**
- **専門用語には初出時に短い説明を添える**（例:「rebase（自分の変更を、最新の共有コードの上に載せ直す操作）」）
- 返答の冒頭に「現在: 手順1-3 / 全3手順」のように現在地を書く
- YMさんはやる気は十分ありますが、AIやプログラミングにはまだ不安があります。**質問が出たら「それは詰まりやすいところです」と受け止めてから答えてください**

> [!warning] 自動承認モードでも、次の2つだけは必ずYMさんに「はい」と言わせてから実行してください
> - 手順1-4 の `git reset --hard`（作業中の変更を捨てます）
> - 手順3-3 の `git push --force-with-lease`
>
> それ以外は確認なしで進めて構いません。

## 0-1. 前回（2026-09-03）に起きたこと

日報とGitHubの状態を突き合わせた結果です。**責める意図はありません。事実だけ書きます。**

1. **rebaseができていませんでした。** 日報に「テスト51件パス」とありますが、**現在のmainは90件**です。51件は8月下旬の数字で、`YMdeveropment` ブランチは8/26のまま、**mainから60コミット遅れています。** 古いコードの上で作業していたので、あちこち噛み合わなかったのだと思います
2. **`meter_reader.py` を書き換えていました。** これは読み取りパイプラインの中核で、**2026-09-01から凍結中**の領域です。またYMさんの担当範囲（共通レコードと保存規約）でもありません
3. **`tests/test_tick_detect_polar.py` のアサーションを緩めていました。** テストが落ちたときにアサーションを緩めると、**退行を検知する仕組みそのものが効かなくなります**

**幸い、すべて未コミットでした。共有コードへの影響はありません。**

**そして重要な点。YMさんが8/26までに作った `orientation.py` は、既にmainに入っています。**しかも8/28に改良版（`min_margin` による誤回転防止）が入っています。**成果は失われていません。**

60コミット遅れのブランチをrebaseするより、mainから作り直す方が確実で速いので、本来のタスクの前に環境を作り直します。

---

# 手順1: 環境を作り直す（前半・必須）

## 1-1. 今の状態を記録する

現状をそのまま残します。

```
cd C:\卒研\git_stk\sotuken
git status
git branch --show-current
git log --oneline -3
```

**この出力を控えておいてください。日報に貼ります。**

## 1-2. 未コミットの変更を退避する

捨てる前に、念のためファイルへ保存します。**リポジトリの外に出すのがポイント**です（中に置くと間違ってコミットされます）。

```
git diff > C:\Users\<自分のユーザー名>\ym_backup_20260908.patch
git status --short
```

## 1-3. 今のブランチを保険として残す

```
git branch backup/YM-20260908
```

これで今のコミット位置に名札が付きます。`origin/YMdeveropment` にも同じものが残っているので、**二重の保険**です。

## 1-4. mainの最新へ揃える

**このコマンドを実行する前に、YMさんに「実行していいですか」と確認してください。**

```
git fetch origin
git checkout YMdeveropment
git reset --hard origin/main
```

`--hard` は作業中の変更を捨てます。1-2で `.patch` に保存し、1-3で名札を付けたので、失われるものはありません。

## 1-5. 揃ったことを確認する

```
git log --oneline -3
venv\Scripts\python.exe -m unittest discover -s tests
```

**`Ran 90 tests ... OK` と出れば成功です。** 件数が90でなければ、そこで止めて大久保に連絡してください。**51件のままなら、まだ古いコードの上にいます。**

## 1-6. 退避した変更について

`ym_backup_20260908.patch` の中身は、**今回は戻しません。**

- `meter_reader.py` の変更 → 凍結中かつ担当範囲外なので戻しません
- `tests/test_tick_detect_polar.py` のアサーション変更 → **退行検知を弱めるので戻しません**

**もしmainの90件のどれかが落ちるようなら、それは本物の問題です。** アサーションを直すのではなく、**そのまま大久保へ報告してください。**

---

# 手順2: YM-02 レコードのデータクラス定義（後半・本来のタスク）

**手順1が90件パスになってから着手してください。** ここまでで時間切れなら、それで構いません。日報にそう書いてください。

これは**設計を考える作業ではなく、転記作業**です。下の表を、項目名・型・nullを許すか・決められた値を落とさずにPythonのデータクラスへ書き写します。新しい項目を考えたり、表の意味を変えたりしないでください。

`dataclass` は決めた項目をひとまとまりで持つためのPythonの書き方、`Optional` は「値が入っていないこともある」という意味です。

```python
from dataclasses import dataclass
from typing import Optional

@dataclass
class Example:
    device_name: Optional[str]
    value: Optional[float]
```

## 2-1. レコード表（`Reading` クラスにする）

1レコードは1回の読み取りを表し、以下を必ず保持します。

| フィールド | 型・値 | 内容 |
|---|---|---|
| reading_id | UUID | 主キー |
| local_id | 文字列またはnull | 端末由来。同じ画像の再処理レコード間で同じ値を持つ |
| captured_at / received_at / processed_at | ISO 8601 | 撮影時刻／PC取込時刻／読み取り実行時刻 |
| device_name | 文字列またはnull | 機器名。未登録・未入力を許容 |
| value | 数値またはnull | 読み取り値。失敗時はnull |
| unit | 文字列またはnull | 単位 |
| status | ok / low_confidence / failed | **自動読み取り**の結果状態。人の入力では変化しない |
| input_method | auto / manual / corrected | **最終的に採用した値**の出所 |
| confidence | 0.0〜1.0またはnull | 読み取りの信頼度 |
| image_path | 相対パス | 保存済み原画像 |
| image_sha256 | SHA-256 | 原画像のハッシュ。重複排除キー |
| failure_stage | null / center / scale / needle / unknown | 失敗段階 |
| failure_code | 文字列またはnull | 短い理由コード |
| failure_detail | 文字列またはnull | 人が読む失敗説明 |
| pipeline_version | 文字列 | 使用したパイプラインの版。gitタグ名をそのまま入れる |
| log_path | 相対パス | 詳細ログ |
| overlay_path | 相対パスまたはnull | 中間オーバーレイ画像 |

> `captured_at / received_at / processed_at` は**1行にまとめて書いてあるだけで、項目は3つ**です。`captured_at`、`received_at`、`processed_at` を別々に定義してください。

## 2-2. 画像に紐づく真値（別表・`ImageTruth` クラスにする）

| フィールド | 型・値 | 内容 |
|---|---|---|
| image_sha256 | SHA-256 | キー |
| operator_value | 数値またはnull | 現場で入力した真値 |
| operator_note | 文字列またはnull | 現場の備考 |
| scale_min | 数値またはnull | 評価に使うフルスケール下限 |
| scale_max | 数値またはnull | 評価に使うフルスケール上限 |
| entered_at | ISO 8601 | 入力時刻 |

## 2-3. 表の背後にある規約（変えないでください）

- 読み取りに失敗しても、必ずレコードを作る。`value` は `null`、`status` は `failed` とし、撮影記録そのものを失わせない
- 原画像は無加工で必ず保存し、SHA-256で一意化する。これは重複排除にも用いる
- 再処理時は既存レコードを上書きしない。同じ `image_sha256` の原画像から、新しい `pipeline_version` を持つ新規レコードを追加する。通常表示では同一画像内の最新を採用し、過去レコードも残して改善前後を比較可能にする
- **`pipeline_version` はgitタグで固定する。** 版を文字列で自由に名乗らせると、どのコードで出た値か後から特定できず、版間比較が成立しない。「版を変えて再処理すると誤差が改善する」ことを数値で示すのが本研究の主張の実証にあたるため、このフィールドは付帯情報ではなく**実験の主キー**である
- **`status` と `input_method` は直交する。** `status` は自動読み取りがどうなったかだけを表し、人が値を入れても書き換えない。`input_method` は最終的に採用した値がどこから来たかを表す。この2つを混ぜると「自動読み取りが失敗したので人が入力した」（`status=failed` かつ `input_method=manual`）が表現できなくなり、**評価に使えるデータが壊れる**。自動読み取りの成否は精度評価の母数そのものなので、人の訂正で消してはならない
- **`operator_value` は画像に紐づき、レコードには紐づかない。** 同じ `image_sha256` に対して1つだけ持つ。レコードごとに複製して食い違わせない。真値は正解データとして評価に使うため、版によって値が変わってはならない
- **`reading_id` はPC側が採番する。端末は `reading_id` を知らない。** サイドカーJSONに載せるのは `local_id` であって `reading_id` ではない
- `failure_stage` は既存 `meter_pipeline.py` が実際に返す `stage` 値に合わせたもの。盤面検出・目盛り検出・OCR・VLMの失敗は現状すべて `scale` に含まれ、区別できない。**細分化はパイプラインの改修（＝精度トラックの凍結解除）を伴うため、今回のスコープ外。**判別できない失敗は `failure_code` に理由文字列を入れて残す

## 2-4. 手順（この順番どおりに1つずつ）

1. `manager/record.py` という新しいファイルを作る
2. 先頭に `from dataclasses import dataclass` と `from typing import Optional` を書く
3. `Reading` クラスを作り、**2-1のレコード表**の項目を**上から順に**書き写す
4. `ImageTruth` クラスを作り、**2-2の真値表**の項目を上から順に書き写す
5. 「文字列またはnull」は `Optional[str]`、「数値またはnull」は `Optional[float]` にする
6. `status` と `input_method` は決められた文字列しか入らないので、**まずは `str` で書き**、コメントに入る値（`ok` / `low_confidence` / `failed` など）を残す
7. `tests/test_record.py` を作る（**`manager/` ではなく `tests/` に置く。** `manager/` に置くと標準のテストコマンドで走りません）
8. テストを実行する

## 2-5. テストに最低限入れること

- `Reading` と `ImageTruth` をそれぞれ作れる
- 読み取り失敗のケースで `value` が `None` のまま保持される
- `status` と `input_method` に別々の値を入れて、混ざらない

## 2-6. 開発環境（正確に守ってください）

| 項目 | 内容 |
|---|---|
| リポジトリの場所 | `C:\卒研\git_stk\sotuken` |
| OS | Windows 11（コマンドは PowerShell） |
| Python | **3.8.8** |
| Pythonの実行 | `venv\Scripts\python.exe`（`python` ではなくこれ） |
| テスト | pytestは入っていません。**標準の unittest** |
| テスト実行 | `venv\Scripts\python.exe -m unittest discover -s tests` |
| コミットメッセージ | 日本語で書く |

**Python 3.8 で動く書き方に限ります。** `list[int]` は使えません（`from typing import List` して `List[int]`、または型注釈を書かない）。`match` 文もありません。`SyntaxError` が出たら、まずバージョン違いを疑ってください。

---

# 手順3: 検証・push・日報

## 3-1. 受け入れ条件

### 前半（必須）

- [ ] `git status` が clean で、ブランチが `YMdeveropment` になっている
- [ ] `git log --oneline -3` の一番上がmainの最新と一致している
- [ ] `venv\Scripts\python.exe -m unittest discover -s tests` が **90件全パス**する
- [ ] `backup/YM-20260908` ブランチが存在する

### 後半（前半が終わってから）

- [ ] `manager/record.py` に `Reading` と `ImageTruth` が**別の型として**定義されている
- [ ] `ImageTruth` が `image_sha256` をキーに1画像につき1つだけ持つ形になっている
- [ ] `status` と `input_method` が別項目で、失敗時の `value=None` を表現できる
- [ ] `tests/test_record.py` があり、テスト件数が90件から増えて全パスする
- [ ] 定義した項目一覧を、2-1・2-2の表と**1行ずつ照合した**

## 3-2. 守ってほしいこと

- **`meter_pipeline.py` `meter_reader.py` `scale_value_detect.py` `tick_detect.py` を変更しない。** 読み取り精度は凍結中で、YMさんの担当範囲でもありません。**YMさんが「このファイルも直したほうがよさそう」と言い出したら、あなたはそれを止めて、大久保に相談するよう促してください**
- **テストが落ちたときに、期待値やアサーションを緩めて通さないでください。** 落ちた理由が分からなければ、**そのまま日報に書いて大久保に相談する。** これが一番大事です
- 企業提供画像・DBファイル（`manager.db`）・出力CSVをコミットしない。**`git add -A` は使わず、変更したファイルを個別に指定して add する**
- `print()` に絵文字を使わない（Windowsのcp932でUnicodeEncodeErrorになる）
- `.patch` ファイルをリポジトリの中に置かない
- 今回は仕様表の転記だけ。設計判断を増やしたり、仕様にない項目を追加したりしない

## 3-3. push前の確認

```
git status
venv\Scripts\python.exe -m unittest discover -s tests
```

テストが全パスすることを確認してから、変更したファイルを**個別に** `git add` してコミットしてください。

**1-4で `reset --hard` をしているので、pushは次を使ってください。**

```
git push --force-with-lease origin YMdeveropment
```

**ただの `--force` は使わないでください。** `stale info` や `rejected` と出たら、そこで止めて大久保に画面を見せてください。

## 3-4. 最後に必ずやること：日報を書く

今回の作業が終わったら（途中で終わった場合も含めて）、日報をMarkdownファイル1枚にまとめてください。ファイル名は `日報_<名前>_<YYYYMMDD>.md` です。**分からない項目は「なし」と書き、空欄にしないでください。**

1. **名前と日付**
2. **今日のタスク名**（例: YM-02 レコードのデータクラス定義）
3. **やったこと** — 実際に手を動かした内容を3〜5行で。着手できなかったことは、そう書く
4. **成果物** — 作成・変更したファイルのパス。コミットしたなら**ハッシュ先頭7文字とブランチ名**も。していないなら「未コミット」
5. **テスト結果** — 実行したコマンドと、何件中何件パスしたか。未実行ならその理由
6. **受け入れ条件の達成状況** — 3-1を1つずつ書き出し、達成／未達成を付ける
7. **詰まったところ・分からなかったこと** — **ここが一番大事です。** 解決できなかった点、判断に迷った点、推測で進めた箇所を正直に。「特になし」で済ませない
8. **次に必要なこと** — 次回やること、または大久保に判断・確認してほしいこと

**手順1-1で取った `git status` と `git log` の出力も貼ってください。**

### 日報を書くときの注意

- **できなかったことを隠さないでください。** 進捗が無かった日も、どこで止まったかが分かれば次の手を打てます。うまくいったように書かれるのが一番困ります
- **推測で断定しないでください。** 確認していないことは「未確認」と書く
- 企業提供画像そのものや、その内容が分かる記述を含めない
- 絵文字は使わない（cp932で文字化けします）

書けたら、Slackチャンネル `#33-iot-ai-team-d` へファイル（.md または .txt）として投稿してください。

---

# 最初にすること

以上を理解したら、次のように応答してください。

1. 「準備できました」と伝える
2. 今日の作業が **前半（環境の作り直し）→ 後半（YM-02 レコード定義）** の2部構成であることを3〜4行で説明する
3. `orientation.py` の成果は既にmainに入っていて**失われていない**ことを伝える
4. **手順1-1だけ**を実行し、その出力をYMさんに見せる

手順1-2以降の説明は、このタイミングではしないでください。
