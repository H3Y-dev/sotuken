# 次回の作業日に配る文面（2026-09-02 作成 / **2026-09-03 配布済み**）

2026-09-02は本人の都合で配布を見送り、**2026-09-03に配布した。**
配布前に「配る前の確認」を実施し、4人とも9/1以降のpushが無いこと・テストが83件のままであることを
実測で確認したため、文面は書き直さずそのまま使った。

> [!important] 2026-09-03の配布方式の変更
> **本文にAIへそのまま貼れるプロンプトを直接載せる方式へ変えた**（従来はGitHubリンクのみ）。
> メンバーがリンクを開かずにAIへ渡せる。GitHubリンクは「詳細版」として併記する。
> あわせて、日報を書かせる指示を全員へ追加配布した（`_依頼文テンプレート.md` の9章が正本）。
>
> **Slackの本文にMarkdownの表を貼らないこと。** 送信は成功するのに表だけ無言で消える。
> 9/3はこれでYMさんへ渡す転記対象の表が丸ごと消え、補足投稿で直した。箇条書きへ展開する。

投稿先: Slack `#33-iot-ai-team-d`

---

## 配る前の確認（3分）

1. `git fetch origin --prune` で4人のブランチに新しいpushが無いか見る。
   あれば中身を読んで、下の文面に一言足す（前回から進んでいる人へ「〇〇を確認しました」）
2. 共通テストの件数が83件のままか確認する。増えていたら文面の数字を直す

```bash
git fetch origin --prune && for b in SKdeveropment SRdevelopment YMdeveropment KPdevelopment; do echo "--- $b"; git log origin/$b -1 --format="%h %ad %s" --date=format:"%m/%d %H:%M"; done
```

---

## 1. まず全員へ（Sprint 1 の招集）

> 今日はまず**全員で集まって、あとで揉めるところを先に決めます**。前半1時間だけです。
>
> 決めるのは3つ。
> 1. Androidの `enqueue` / `observeQueue` の引数・戻り値・状態名
> 2. 端末からPCへ渡すサイドカーJSONの項目名
> 3. PC側の監視フォルダの命名規則
>
> 議題と叩き台はここにあります。**ゼロから議論せず、「この形でよいか」「変えるならどこか」だけ詰めます。**
> https://github.com/H3Y-dev/sotuken/blob/main/tasks/spec/2026-09-02_Sprint1_インターフェース合意メモ.md
>
> 前回（T0-2）はここを飛ばしたせいで、せっかく書いたモジュールがパイプラインに繋がらず、
> CSV出力が2人ぶん重複して作られました。同じことを繰り返さないための1時間です。
>
> 後半2時間は各自の環境準備です。合意メモの最後にチェックリストがあります。
>
> **決まったことは、その場で合意メモの「決定」欄に書き込みます。** ここが唯一の正になります。

## 2. 各メンバーへ（Sprint 1 の後に個別で渡す）

各メッセージは、本文を貼るのではなくGitHubのリンクを渡す運用（本文が長くSlackに収まらないため）。
メンバーは自分のAIとの会話に、リンク先の区切り線から下を貼り付ける。

### SKくん（佐藤蓮大）

> SKくん、今日はAndroidアプリの土台をSKくんの端末で動かすところまでです。
> **アプリを新しく作る必要はありません。**もう用意してある雛形をビルドして起動するだけです。
> UIを作り込むのは次回からで、今日は「この端末で確実に開く」ことを確認します。
>
> https://github.com/H3Y-dev/sotuken/blob/main/tasks/prompts/SKくん-Android雛形起動-20260902.md
>
> 起動できたらスクリーンショットを送ってください。

### SRくん（佐々木綾杜）

> SRくん、今日は**まずrebaseからです**。`SRdevelopment` が main からかなり遅れているので、
> ここを片付けないと先に進めません。手順はリンク先に1コマンドずつ書いてあります。
> **競合が出たら自分で直そうとせず、画面のスクショを撮って送ってください。**一緒に見ます。
>
> そのあとは、PC側の監視フォルダの流れを1枚にまとめる作業です。**今日はコードを書きません。**
> 今ある `manager/batch.py` の何が使えて何が未完かを整理するところまでです。
>
> https://github.com/H3Y-dev/sotuken/blob/main/tasks/prompts/SRくん-監視フォルダ設計-20260902.md

### YMさん（山内はるか）

> YMさん、今日は**仕様書の表をそのままPythonに書き写す作業**です。
> 何を作るか考える必要はありません。手順が8ステップに分けて書いてあるので、上から順にやれば終わります。
>
> https://github.com/H3Y-dev/sotuken/blob/main/tasks/prompts/YMさん-レコード定義-20260902.md
>
> 1つだけ大事なところがあります。「読み取りが失敗したこと」と「人が手で値を入れたこと」を、
> **別々の項目として持たせてください。**ここを一緒にすると、あとで「AIが何枚失敗したか」を
> 数えられなくなります。リンク先に理由も書いてあります。

### KPくん（鈴木桔平）

> KPくん、前回の `readings_to_series` 動作確認できました。お疲れさまでした。
> mainにも取り込み済みです。
>
> 今日は、KPくんがこれまでに作った3つの関数（絞り込み・機器別まとめ・グラフ用変換）を、
> 新しい記録の形に合わせる作業です。**触るのは `manager/export.py` の3関数だけです。**
>
> https://github.com/H3Y-dev/sotuken/blob/main/tasks/prompts/KPくん-新スキーマ対応-20260902.md
>
> YMさんも似た作業をしていますが、**YMさんの完成を待つ必要はありません。**
> 見るのは仕様書の表なので、手書きのダミーデータで先に進められます。

## 3. 全員への共通連絡（末尾に付ける）

> - 着手前に `git fetch origin && git rebase origin/main`
> - 共通テストは**83件**です（前回まで50〜77件と伝えていましたが、KPくんの `manager/export.py` を
>   mainへ取り込んだので増えています）
> - 企業提供画像は外に出さない。GitHubへのコミット、生成AIへのアップロードは禁止です
> - 1つ終わったら報告してください。次を渡します

---

## 補足: この日の状態（次回、状況説明に使う）

- 読み取り精度は**凍結中**。精度側の作業は誰も新たに始めない
- v0〜v5のgitタグを打ち、版間比較の表が出せる状態になった（O-08・O-09完了）
- `manager/export.py` はmainに入った。`MeterManager.export_to_csv` との一本化はSprint 3（KP-03）で行う
