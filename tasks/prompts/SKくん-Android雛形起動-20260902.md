# このファイルの使い方

1. これはSKくんとの**新しい会話**に貼る作業指示です。
2. 下の区切り線より下を**全部コピー**して、SKくんが使っているAIの会話にそのまま貼り付ける。

---

あなた（SKくんの担当タスクを支援しているAI）へ。

日本製鋼所様の工場にある丸型アナログ計器をAndroidで撮影し、PC側の既存Python（`meter_pipeline.py`）で読み取り、CSV/JSON Linesへ記録・集約する卒業研究です。11月頃に、撮影から集約までを通しでデモします。
2026-09-01に読み取り精度は現状で凍結し、いまは撮影から集約までを通しで動かす方針です。Sprint 2はSprint 1のインターフェース合意後に始めます。
5人チームで、本人（大久保）はAndroidキューと全体レビュー、SKくんはAndroidのUI、SRくんはPC側取り込み、YMさんは共通レコードと保存規約、KPくんは出力・一覧・境界を担当します。
SKくんの今回の担当範囲は、Android雛形を実機で起動して確認することだけです。

## 1. 今日やること：SK-02 用意済みAndroid雛形を実機で起動する

渡された雛形をビルドして、実機で起動してください。**アプリを新規作成しない、雛形を作り直さない、機能を追加しない**でください。今回のゴールは、渡された土台がSKくんの端末で動くことを確認するだけです。

特に、カメラ権限の**実行時リクエスト**を実装してはいけません。これはSprint 3のSK-03の範囲です。今回の雛形はまだカメラを使わないため、実行時の権限画面も表示されません。

`android/app/src/main/kotlin/jp/sotuken/metercapture/ui/` 配下がSKくんの担当領域です。ただし今回は原則として変更しません。`queue/` 配下は本人（大久保）の担当なので、読んでも変更・作り直しはしないでください。

## 2. なぜ必要か

工場で撮影担当者が計器の前に立ったとき、アプリ自体が端末で開かなければ、撮影もPCへの受け渡しも始められません。今は画面を作り込む段階ではなく、「この端末で土台が確実に開く」と先に確認し、次の撮影機能を安心して載せられる状態にするための作業です。

## 3. Android Studioでの手順（`android/README.md` の要約）

1. Android Studioを入れ、初回設定は `Standard` を選んでAndroid SDKとPlatform-Toolsを入れます。
2. Android Studioで **リポジトリのルートではなく** `C:\卒研\git_stk\sotuken\android` を開きます。
3. SDKを求められたら場所を指定し、`Tools > SDK Manager` で `Android 15.0 (API 35)` のSDK Platformを入れます。JDKはAndroid Studio同梱のJDK 17を使います。
4. `Sync Now` でGradle同期を終え、`Build > Make Project` でビルドします。コマンドなら、PowerShellで `cd C:\卒研\git_stk\sotuken\android` の後に `./gradlew.bat assembleDebug` です。
5. Android端末で開発者向けオプションとUSBデバッグを有効にし、USB接続後に端末側のRSAキー許可をします。
6. Android Studioのデバイス選択で端末を選び、Runを押します。起動後に「メーター撮影」と「キュー: 0件」が表示されることを確認します。

## 4. 受け入れ条件

- [ ] 雛形が実機で起動し、プレースホルダー画面（タイトルとキュー0件）が表示される
- [ ] 保存先設定が有効な状態で、起動結果をスクリーンショットで共有できる

## 5. 検証のしかた

Android Studioから雛形をビルドし、実機で起動して、プレースホルダー画面を目視してください。保存先設定が有効であることも確認し、起動結果のスクリーンショットを本人（大久保）へ共有してください。

このタスクはAndroid側の起動確認なので、PC側の共通テストコマンドは対象外です。

## 6. 守ってほしいこと

- 着手前に必ず `git fetch origin` と `git rebase origin/main` を実行する
- 企業提供画像・DBファイル（`manager.db`）・出力CSVをコミットしない。`git add -A` は使わず、変更したファイルを個別に指定して add する
- `print()` に絵文字を使わない（Windowsのcp932でUnicodeEncodeErrorになる）
- 分からないことがあれば本人（大久保）に聞く
- 今回は雛形を起動するだけです。新規アプリ作成、雛形の作り直し、`queue/` の変更、カメラ権限の実行時リクエスト実装はしないでください

## 7. push前の確認

```text
git status
```

`git status` に見覚えのないファイル、特に企業提供画像・`manager.db`・出力CSVが出ていないか確認してください。今回はコード変更が不要なら、コミットやpushはしないで本人へ起動結果を共有してください。

## 8. 最後に必ずやること：日報を書く

今日の作業が終わったら（途中で終わった場合も含めて）、日報をMarkdownファイル1枚にまとめてください。
ファイル名は `日報_<名前>_<YYYYMMDD>.md` です。書く内容は次の8項目です。
**分からない項目は「なし」と書き、空欄にしないでください。**

1. **名前と日付**
2. **今日のタスク名**（タスクIDを付ける。例: SK-02 Android雛形を実機で起動する）
3. **やったこと** — 実際に手を動かした内容を3〜5行で。やろうとしたが着手できなかったことは、そう書く
4. **成果物** — 作成・変更したファイルのパス。コミットしたなら**コミットのハッシュ（先頭7文字）と
   ブランチ名**も書く。コミットしていないなら「未コミット」と書く
5. **テスト結果** — 実行したコマンドと、何件中何件パスしたか。実行していないなら「未実行」と、その理由
6. **受け入れ条件の達成状況** — 渡された受け入れ条件を1つずつ書き出し、達成／未達成を付ける
7. **詰まったところ・分からなかったこと** — **ここが一番大事です。** 解決できなかった点、判断に迷った点、
   推測で進めた箇所を正直に書いてください。「特になし」で済ませず、少しでも自信のない箇所は書き出してください
8. **次に必要なこと** — 次回やること、または大久保に判断・確認してほしいこと

### 日報を書くときの注意

- **できなかったことを隠さないでください。** 進捗が無かった日も、どこで止まったかが分かれば次の手を打てます。
  うまくいったように書かれるのが一番困ります
- **推測で断定しないでください。** 確認していないことは「未確認」と書いてください
- 企業提供画像そのものや、その内容が分かる記述を日報に含めないでください
- 絵文字は使わないでください（Windowsのcp932で文字化けします）

書けたら、Slackチャンネル `#33-iot-ai-team-d` へファイル（.md または .txt）として投稿してください。
