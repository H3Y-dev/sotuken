# このファイルの使い方

1. これはYMさんとの**新しい会話**に貼る作業指示です。
2. 下の区切り線より下を**全部コピー**して、YMさんが使っているAIの会話にそのまま貼り付ける。

**この指示は2026-09-03の作業終了時に作りました。次回の作業日に使います。**

> [!important] 前半（1章）は大久保と一緒にやることを推奨します
> ブランチを作り直す作業です。手順どおりなら失われるものはありませんが、
> 不安があれば作業日に画面を並べてやりましょう。家で進める場合は、
> **各コマンドの出力をコピーしておいてください。**

---

あなた（YMさんの担当タスクを支援しているAI）へ。

日本製鋼所様の工場にある丸型アナログ計器をAndroidで撮影し、PC側の既存Python（`meter_pipeline.py`）で読み取り、CSV/JSON Linesへ記録・集約する卒業研究です。11月頃に、撮影から集約までを通しでデモします。
2026-09-01に読み取り精度は現状で凍結し、いまは撮影から集約までを通しで動かす方針です。
5人チームで、本人（大久保）はAndroidキューと全体レビュー、SKくんはAndroidのUI、SRくんはPC側取り込み、YMさんは共通レコードと保存規約、KPくんは出力・一覧・境界を担当します。

## 0. 前回（2026-09-03）に起きたこと

日報を読み、GitHubの状態と突き合わせました。**責める意図はありません。事実だけ書きます。**

1. **rebaseができていませんでした。** 日報に「テスト51件パス」とありますが、**現在のmainは83件**です。51件は8月下旬の数字で、YMさんのブランチ `YMdeveropment` は8/26のまま、**mainから44コミット遅れています。** 古いコードの上で作業していたので、あちこち噛み合わなかったのだと思います
2. **`meter_reader.py` を書き換えていました。** これは読み取りパイプラインの中核で、**2026-09-01から凍結中**の領域です。またYMさんの担当範囲（共通レコードと保存規約）でもありません
3. **`tests/test_tick_detect_polar.py` のアサーションを緩めていました。** テストが落ちたときにアサーションを緩めると、**退行を検知する仕組みそのものが効かなくなります。** 落ちた原因は、古いブランチで動かしたことである可能性が高いです

**幸い、すべて未コミットでした。共有コードへの影響はありません。**

**そして重要な点。YMさんが8/26までに作った `orientation.py` は、既にmainに入っています。**しかも8/28に改良版（`min_margin` による誤回転防止）が入っています。**成果は失われていません。**

**なので今回は、本来のタスクの前に環境を作り直します。** 44コミット遅れのブランチをrebaseするより、mainから作り直す方が確実で速いです。

## 1. 前半：環境を作り直す

### 1-1. 今の状態を記録する

まず現状をそのまま残します。リポジトリのルートで実行してください。

```
cd C:\卒研\git_stk\sotuken
git status
git branch --show-current
git log --oneline -3
```

**この出力をコピーして、日報に貼ってください。** 後から何が起きたか追えるようにするためです。

### 1-2. 未コミットの変更を退避する

**捨てる前に、念のためファイルへ保存します。** リポジトリの外に出すのがポイントです。

```
git diff > C:\Users\<自分のユーザー名>\ym_backup_20260903.patch
git status --short
```

`.patch` ファイルができたことを確認してください。**リポジトリの中に置かないでください**（間違ってコミットされます）。

### 1-3. 今のブランチを保険として残す

```
git branch backup/YM-20260903
```

これで今のコミット位置に名札が付きます。**何かあってもここへ戻れます。** さらに `origin/YMdeveropment` にも同じものが残っているので、二重の保険があります。

### 1-4. mainの最新へ揃える

```
git fetch origin
git checkout YMdeveropment
git reset --hard origin/main
```

**`--hard` は作業中の変更を捨てます。** 1-2で `.patch` に保存し、1-3で名札を付けたので、失われるものはありません。

### 1-5. 揃ったことを確認する

```
git log --oneline -3
venv\Scripts\python.exe -m unittest discover -s tests
```

**`Ran 83 tests ... OK` と出れば成功です。** 件数が83でなければ、そこで止めて大久保に連絡してください。**51件のままなら、まだ古いコードの上にいます。**

### 1-6. 退避した変更について

`ym_backup_20260903.patch` の中身は、**今回は戻しません。**

- `meter_reader.py` の変更 → 凍結中かつ担当範囲外なので戻しません
- `tests/test_tick_detect_polar.py` のアサーション変更 → **退行検知を弱めるので戻しません**

**もしmainの83件のどれかが落ちるようなら、それは本物の問題です。** アサーションを直すのではなく、**そのまま大久保へ報告してください。**

## 2. 後半：YM-02（本来のタスク）

**環境が83件パスになってから着手してください。** ここまでで時間切れなら、それで構いません。日報にそう書いてください。

### やること

`manager/record.py` を新規作成し、**レコードと真値を別々のデータクラスとして定義**します。

- `Reading` — 1枚の写真から読み取った結果のレコード
- `ImageTruth` — 人が確認した真値。**`image_sha256` をキーに、1枚の画像につき1つだけ持つ**

**項目名と型の詳細は、前回渡した `tasks/prompts/YMさん-レコード定義-20260902.md` にあります。** 同じ内容なので、そちらを開いて仕様表を見てください。今回はその表をPythonのデータクラスへ書き写す作業です。

### 特に注意する2点

1. **`status` と `input_method` を混同しないでください。** `status` は「読み取りが成功したか失敗したか」、`input_method` は「自動で読んだか人が手入力したか」です。**別々の項目**です
2. **読み取りに失敗したときに `value = None` を表現できるようにしてください。** 失敗を0や空文字で表さないこと

### テストも書く

`tests/test_record.py` を新規作成してください。**`manager/` ではなく `tests/` に置いてください**（`manager/` に置くと標準のテストコマンドで走りません）。

最低限、次を確認するテストを書いてください。

- `Reading` と `ImageTruth` をそれぞれ作れる
- 読み取り失敗のケースで `value` が `None` のまま保持される
- `status` と `input_method` に別々の値を入れて、混ざらない

## 3. 受け入れ条件

### 前半（必須）

- [ ] `git status` が clean で、ブランチが `YMdeveropment` になっている
- [ ] `git log --oneline -3` の一番上がmainの最新と一致している
- [ ] `venv\Scripts\python.exe -m unittest discover -s tests` が **83件全パス**する
- [ ] `backup/YM-20260903` ブランチが存在する

### 後半（前半が終わってから）

- [ ] `manager/record.py` に `Reading` と `ImageTruth` が別の型として定義されている
- [ ] `ImageTruth` が `image_sha256` をキーに1画像につき1つだけ持つ形になっている
- [ ] `status` と `input_method` が別項目で、失敗時の `value=None` を表現できる
- [ ] `tests/test_record.py` があり、テスト件数が83件から増えて全パスする

## 4. 守ってほしいこと

- **`meter_pipeline.py` `meter_reader.py` `scale_value_detect.py` `tick_detect.py` を変更しない。** 読み取り精度は凍結中で、YMさんの担当範囲でもありません
- **テストが落ちたときに、期待値やアサーションを緩めて通さないでください。** 落ちた理由が分からなければ、**そのまま日報に書いて大久保に相談してください。** これが一番大事です
- 企業提供画像・DBファイル（`manager.db`）・出力CSVをコミットしない。`git add -A` は使わず、変更したファイルを個別に指定して add する
- `print()` に絵文字を使わない（Windowsのcp932でUnicodeEncodeErrorになる）
- `.patch` ファイルをリポジトリの中に置かない
- 分からないことがあれば大久保に聞く

## 5. push前の確認

```text
git status
venv\Scripts\python.exe -m unittest discover -s tests
```

テストが全パスすることを確認してから、変更したファイルを個別に `git add` してコミット・pushしてください。

**1-4で `reset --hard` をしているので、pushのときは `git push --force-with-lease origin YMdeveropment` を使ってください。** ただの `--force` は使わないでください。**`stale info` や `rejected` と出たら、そこで止めて大久保に画面を見せてください。**

## 6. 最後に必ずやること：日報を書く

今回の作業が終わったら（途中で終わった場合も含めて）、日報をMarkdownファイル1枚にまとめてください。
ファイル名は `日報_<名前>_<YYYYMMDD>.md` です。書く内容は次の8項目です。
**分からない項目は「なし」と書き、空欄にしないでください。**

1. **名前と日付**
2. **今日のタスク名**（タスクIDを付ける。例: YM-02 レコードのデータクラス定義）
3. **やったこと** — 実際に手を動かした内容を3〜5行で。やろうとしたが着手できなかったことは、そう書く
4. **成果物** — 作成・変更したファイルのパス。コミットしたなら**コミットのハッシュ（先頭7文字）と
   ブランチ名**も書く。コミットしていないなら「未コミット」と書く
5. **テスト結果** — 実行したコマンドと、何件中何件パスしたか。実行していないなら「未実行」と、その理由
6. **受け入れ条件の達成状況** — 渡された受け入れ条件を1つずつ書き出し、達成／未達成を付ける
7. **詰まったところ・分からなかったこと** — **ここが一番大事です。** 解決できなかった点、判断に迷った点、
   推測で進めた箇所を正直に書いてください。「特になし」で済ませず、少しでも自信のない箇所は書き出してください
8. **次に必要なこと** — 次回やること、または大久保に判断・確認してほしいこと

**今回は1-1で取った `git status` と `git log` の出力も貼ってください。**

### 日報を書くときの注意

- **できなかったことを隠さないでください。** 進捗が無かった日も、どこで止まったかが分かれば次の手を打てます。
  うまくいったように書かれるのが一番困ります
- **推測で断定しないでください。** 確認していないことは「未確認」と書いてください
- 企業提供画像そのものや、その内容が分かる記述を日報に含めないでください
- 絵文字は使わないでください（Windowsのcp932で文字化けします）

書けたら、Slackチャンネル `#33-iot-ai-team-d` へファイル（.md または .txt）として投稿してください。
