# このファイルの使い方

1. これはSRくんとの**継続中の会話**に貼るフォローアップです（`SRくん-修正依頼-20260825.md`の続き。絡まったコンフリクト履歴を避けるため、`SRdevelopment-v2`という新しいブランチをorigin/mainから作り直した後の作業手順）。
2. 下の区切り線より下を全部コピーして、SRくんが使っているGeminiの会話にそのまま貼り付ける。

---

あなた（SRくんの担当タスクを支援しているAI）へ。`SRdevelopment`ブランチは、`git rebase`のたびに同じコンフリクトが繰り返し起きる状態（「SRの作業内容」「T6-3完了」という似た内容のコミットが複数回重なっていた）になっていたため、チームリーダーの判断で**`SRdevelopment-v2`という新しいブランチを、最新の`origin/main`から作り直しました。** SRくんはすでにこのブランチをローカルで作成・切り替え済みです。

このブランチには絡まった履歴がなく、コンフリクトの心配なく作業できます。ここから、SRくんの本当に新しかった作業（`main.py`の追加、`manager.py`への`export_to_csv`追加）を、クリーンな状態でもう一度作ってもらいます。

## 1. 今の状態を確認する

まず、正しいブランチにいることを確認してください。

```powershell
git branch --show-current
```

`SRdevelopment-v2`と表示されればOKです。違うブランチが表示されたら、先に`git checkout SRdevelopment-v2`を実行してください。

## 2. `main.py`を新規作成する

リポジトリの一番上の階層（`sotuken`フォルダ直下、`manager`フォルダの外）に、`main.py`という名前で以下の内容をそのまま保存してください。

```python
import argparse
import os
import sys

# 1. カレントディレクトリをパスに追加
current_dir = os.path.dirname(os.path.abspath(__file__))
if current_dir not in sys.path:
    sys.path.insert(0, current_dir)

# 2. manager パッケージから MeterManager をインポート
from manager.manager import MeterManager

# 3. read_meter のインポート（未実装時のモック対応）
try:
    from reader import read_meter
except ImportError:
    try:
        from read_meter import read_meter
    except ImportError:

        def read_meter(image_path: str):
            print(f"[INFO] Mock read_meter Executing for: {image_path}")
            return {
                "stage": "ok",
                "value": 42.5,
                "ratio": 0.425,
                "angle_deg": 120.0,
                "error": None,
            }


def main():
    parser = argparse.ArgumentParser(
        description="アナログメーター自動読み取りシステム CLI"
    )
    parser.add_argument(
        "--image", "-i", type=str, help="読み取るメーター画像のパス"
    )
    parser.add_argument(
        "--device",
        "-d",
        type=str,
        default="Default_Device",
        help="メーターの識別名 (例: Pressure_Gauge_01)",
    )
    parser.add_argument(
        "--db",
        type=str,
        default="manager.db",
        help="使用するSQLite DBのファイルパス",
    )
    parser.add_argument(
        "--history", action="store_true", help="全読み取り履歴を表示"
    )
    parser.add_argument(
        "--export-csv",
        type=str,
        nargs="?",
        const="readings_export.csv",
        help="履歴をCSVファイルに出力（パス指定省略時は readings_export.csv）",
    )

    args = parser.parse_args()
    manager = MeterManager(db_path=args.db)

    if args.image:
        if not os.path.exists(args.image) and "Mock" not in str(read_meter):
            print(f"[ERROR] 指定された画像ファイルが存在しません: {args.image}")
            sys.exit(1)

        print(f"--- 画像読み取り開始: {args.image} (Device: {args.device}) ---")
        result = manager.process_image(
            image_path=args.image,
            device_name=args.device,
            reader_func=read_meter,
        )

        print("\n【処理結果】")
        print(f"  ID        : {result.id}")
        print(f"  Timestamp : {result.timestamp}")
        print(f"  Device    : {result.device_name}")
        print(f"  Stage     : {result.stage}")
        print(f"  Value     : {result.value if result.value is not None else 'N/A'}")

    if args.export_csv:
        csv_path = manager.export_to_csv(args.export_csv)
        print(f"\n[INFO] 履歴データをCSVに出力しました: {csv_path}")

    if args.history or (not args.image and not args.export_csv):
        print("\n【全読み取り履歴】")
        print(manager.format_history_for_cli())


if __name__ == "__main__":
    main()
```

これは以前のブランチにあったものと同じ内容です（設計自体は問題なかったので、そのまま復元しています）。

## 3. `manager/manager.py`に`export_to_csv`を追加する

既存の`manager/manager.py`を開き、ファイル先頭の`import`部分に`import csv`を追加してください。

```python
import csv
import os
import sys
from typing import Any, Dict, List, Optional
```

そして、`format_history_for_ui`メソッドの**後**（クラスの最後）に、次のメソッドを追記してください。

```python
    def export_to_csv(self, output_path: str = "readings_export.csv") -> str:
        """保存されている全履歴をCSVファイルに出力する"""
        readings = self.get_history()
        fieldnames = ["id", "timestamp", "device_name", "stage", "value", "image_path"]

        with open(output_path, mode="w", newline="", encoding="utf-8-sig") as f:
            writer = csv.DictWriter(f, fieldnames=fieldnames)
            writer.writeheader()
            for r in readings:
                writer.writerow(
                    {
                        "id": r.id,
                        "timestamp": r.timestamp,
                        "device_name": r.device_name,
                        "stage": r.stage,
                        "value": r.value if r.value is not None else "",
                        "image_path": r.image_path,
                    }
                )
        return os.path.abspath(output_path)
```

**インポートの書き方は変更しないでください。** ファイルの先頭にある

```python
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from storage import MeterReading, Storage
```

はこのままにしておいてください（`from manager.storage import ...`のような書き方に変えると、以前と同じ理由で`test_integration.py`が壊れます）。

## 4. データファイルをgit管理から除外する

`.gitignore`を開き、末尾に次の2行を追加してください。

```
manager.db
readings_export.csv
```

（これらは実行するたびに生成されるデータファイルなので、コードと一緒にはコミットしません。）

## 5. テストで確認する

```powershell
venv\Scripts\python.exe -m unittest discover -s manager -p "test_*.py"
```

**`OK`と表示されることを必ず確認してから、次に進んでください。** エラーが出たら、2〜4のどこかでタイプミスがある可能性が高いです。エラーメッセージ全文をこちらに貼ってもらえれば、一緒に確認します。

## 6. 動作確認（任意だが推奨）

実際にCLIを動かしてみると、正しく動いていることが目で確認できます。

```powershell
venv\Scripts\python.exe main.py --history
```

（この時点では記録が無いので「履歴データがありません。」と出れば正常です。エラーで止まらなければOK。）

## 7. コミット・プッシュ

VS Codeのソース管理パネルから:

1. 変更した/追加したファイル（`main.py`、`manager/manager.py`、`.gitignore`）をステージ
2. コミットメッセージを日本語で入力（例: `main.pyとexport_to_csvを追加、データファイルをgitignoreに追加`）
3. コミット
4. プッシュ（初回は「リモートブランチを作成しますか？」と聞かれるので「OK」を選ぶ）

## 8. 完成の定義

- [ ] `main.py`が動く（`python main.py --history`がエラーなく実行できる）
- [ ] `manager.py`に`export_to_csv`が追加されている
- [ ] `.gitignore`に`manager.db`/`readings_export.csv`が追加されている
- [ ] `venv\Scripts\python.exe -m unittest discover -s manager -p "test_*.py"`が`OK`
- [ ] `SRdevelopment-v2`としてGitHubにpushできている

## 9. 開発環境（前回と同じ）

| 項目 | 内容 |
|---|---|
| リポジトリ | `C:\卒研\git_stk\sotuken`（Windows 11 / PowerShell） |
| Python | **3.8.8**。`venv\Scripts\python.exe` |
| コミットメッセージ | 日本語で |
| 触ってよいファイル | `manager/`配下、リポジトリ直下の`main.py`、`.gitignore` |
| 絶対に変更しないファイル | `tick_detect.py` / `scale_value_detect.py` / `vlm_scale_value.py` / `meter_reader.py` / `meter_pipeline.py` / `evaluate.py` / `main_sotuken.py` / `tests/` / `eval/` |

ここまで終わったら、チームリーダーにその旨を伝えてください。プッシュされた内容を確認し、問題なければmainへマージします。
