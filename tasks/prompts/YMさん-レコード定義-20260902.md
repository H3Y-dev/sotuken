# このファイルの使い方

1. これはYMさんとの**新しい会話**に貼る作業指示です。
2. 下の区切り線より下を**全部コピー**して、YMさんが使っているAIの会話にそのまま貼り付ける。

---

あなた（YMさんの担当タスクを支援しているAI）へ。

日本製鋼所様の工場にある丸型アナログ計器をAndroidで撮影し、PC側の既存Python（`meter_pipeline.py`）で読み取り、CSV/JSON Linesへ記録・集約する卒業研究です。11月頃に、撮影から集約までを通しでデモします。
2026-09-01に読み取り精度は現状で凍結し、いまは撮影から集約までを通しで動かす方針です。Sprint 2はSprint 1のインターフェース合意後に始めます。
5人チームで、本人（大久保）はAndroidキューと全体レビュー、SKくんはAndroidのUI、SRくんはPC側取り込み、YMさんは共通レコードと保存規約、KPくんは出力・一覧・境界を担当します。
YMさんの今回の担当範囲は、仕様書の表をPythonのデータクラスへそのまま書き写すことです。

## 1. 今日やること：YM-02 レコードのデータクラス定義を仕様表どおりに作る

これは**設計を考える作業ではなく、転記作業**です。仕様書第2節の表を、項目名・型・nullを許すか・決められた値を落とさずにPythonのデータクラスへ書き写してください。新しい項目を考えたり、表の意味を変えたりする必要はありません。

ファイルの置き場所は `manager/record.py` を提案します。`dataclass` は、決めた項目をひとまとまりで持つためのPythonの書き方です。`Optional` は「値が入っていないこともある」という意味です。

```python
from dataclasses import dataclass
from typing import Optional

@dataclass
class Example:
    device_name: Optional[str]
    value: Optional[float]
```

## 2. 仕様書第2節：この表をそのまま書き写す

1レコードは1回の読み取りを表し、以下を必ず保持する。

| フィールド | 型・値 | 内容 |
|---|---|---|
| reading_id | UUID | 主キー |
| local_id | 文字列またはnull | 端末由来。同じ画像の再処理レコード間で同じ値を持つ |
| captured_at / received_at / processed_at | ISO 8601 | 撮影時刻／PC取込時刻／読み取り実行時刻 |
| device_name | 文字列またはnull | 機器名。未登録・未入力を許容 |
| value | 数値またはnull | 読み取り値。失敗時はnull |
| unit | 文字列またはnull | 単位 |
| status | ok / low_confidence / failed | **自動読み取り**の結果状態。人の入力では変化しない |
| input_method | auto / manual / corrected | **最終的に採用した値**の出所 |
| confidence | 0.0〜1.0またはnull | 読み取りの信頼度 |
| image_path | 相対パス | 保存済み原画像 |
| image_sha256 | SHA-256 | 原画像のハッシュ。重複排除キー |
| failure_stage | null / center / scale / needle / unknown | 失敗段階 |
| failure_code | 文字列またはnull | 短い理由コード |
| failure_detail | 文字列またはnull | 人が読む失敗説明 |
| pipeline_version | 文字列 | 使用したパイプラインの版。gitタグ名をそのまま入れる |
| log_path | 相対パス | 詳細ログ |
| overlay_path | 相対パスまたはnull | 中間オーバーレイ画像 |

### 画像に紐づく真値（別表）

| フィールド | 型・値 | 内容 |
|---|---|---|
| image_sha256 | SHA-256 | キー |
| operator_value | 数値またはnull | 現場で入力した真値 |
| operator_note | 文字列またはnull | 現場の備考 |
| scale_min | 数値またはnull | 評価に使うフルスケール下限 |
| scale_max | 数値またはnull | 評価に使うフルスケール上限 |
| entered_at | ISO 8601 | 入力時刻 |

- 読み取りに失敗しても、必ずレコードを作る。`value` は `null`、`status` は `failed` とし、撮影記録そのものを失わせない。
- 原画像は無加工で必ず保存し、SHA-256で一意化する。これは重複排除にも用いる。
- 再処理時は既存レコードを上書きしない。同じ `image_sha256` の原画像から、新しい `pipeline_version` を持つ新規レコードを追加する。通常表示では同一画像内の最新を採用し、過去レコードも残して改善前後を比較可能にする。
- **`pipeline_version` はgitタグで固定する。** 版を文字列で自由に名乗らせると、どのコードで出た値か後から特定できず、版間比較が成立しない。過去の版にも遡ってタグを打つ（`tasks/plan-android-sprints.md` のO-08）。この「版を変えて再処理すると誤差が改善する」ことを数値で示すのが本研究の主張の実証にあたるため、このフィールドは記録の付帯情報ではなく実験の主キーである。
- **`status` と `input_method` は直交する。** `status` は自動読み取りがどうなったかだけを表し、人が値を入れても書き換えない。`input_method` は最終的に採用した値がどこから来たかを表す。この2つを混ぜると「自動読み取りが失敗したので人が入力した」（`status=failed` かつ `input_method=manual`）が表現できなくなり、**評価に使えるデータが壊れる**。自動読み取りの成否は精度評価の母数そのものなので、人の訂正で消してはならない。
- **`operator_value`（現場で入力された真値）は画像に紐づき、レコードには紐づかない。** 同じ `image_sha256` に対して1つだけ持つ。再処理で新レコードを追加するときは、その画像に紐づく真値を全レコードから参照できるようにする（レコードごとに複製して食い違わせない）。真値は正解データとして評価に使うため、版によって値が変わってはならない。
- **`reading_id` はPC側が採番する。端末は `reading_id` を知らない。**サイドカーJSONに載せるのは `local_id` であって `reading_id` ではない。
- `failure_stage` は既存 `meter_pipeline.py` が実際に返す `stage` 値に合わせたもの。盤面検出・目盛り検出・OCR・VLMの失敗は現状すべて `scale` に含まれ、区別できない。**細分化はパイプラインの改修（＝精度トラックの凍結解除）を伴うため、今回のスコープ外とする。**判別できない失敗は `failure_code` に理由文字列を入れて残す。

## 3. 手順（この順番どおりに1つずつ）

1. `manager/record.py` という新しいファイルを作る
2. ファイルの先頭に `from dataclasses import dataclass` と `from typing import Optional` を書く
3. `Reading` というクラスを1つ作り、上の**レコード表**の項目を**上から順に**書き写す
4. `ImageTruth` というクラスをもう1つ作り、**真値の別表**の項目を上から順に書き写す
5. 「文字列またはnull」と書かれている項目は `Optional[str]`、「数値またはnull」は `Optional[float]` にする
6. `status` と `input_method` は決められた文字列しか入らないので、まずは `str` で書き、コメントに入る値（`ok` / `low_confidence` / `failed` など）を残す
7. `tests/test_record.py` を作り、両方のクラスを1件ずつ作れることを確かめるテストを2つ書く
8. テストを実行する（下の「6. 検証のしかた」）

> 表の `captured_at / received_at / processed_at` は**1行にまとめて書いてあるだけで、項目は3つ**です。`captured_at`、`received_at`、`processed_at` を別々に定義してください。

## 4. 必ず別のクラスにする2点

1. 上のレコード表と「画像に紐づく真値（別表）」は、**別のクラス**として定義してください。真値は `image_sha256` をキーにして、画像1枚につき1つだけ持ちます。
2. `status` と `input_method` は、**別の項目**にしてください。読み取り失敗時に `value` を `None` にできるようにしてください。

## 5. なぜ必要か

工場で自動読み取りが失敗し、担当者が計器を見て手で正しい値を入れる場面があります。このとき `status` と `input_method` を混ぜると、「自動では失敗したが、人が手で入れた」という大事な記録が後から分からなくなります。そうなると、どの画像でAIが失敗したかを数えられず、評価に使えるデータが壊れます。真値もレコードごとに複製すると、同じ写真を再処理したときに正解が食い違ってしまいます。

## 6. 受け入れ条件

- [ ] レコード表と真値表を**別の型として**定義し、真値は `image_sha256` をキーに1つだけ持つ
- [ ] `status` と `input_method` を別項目にし、失敗時の `value=null` を表現できる

## 7. 検証のしかた

定義した項目一覧を、上の仕様書第2節の表と1行ずつ照合してください。次にPC側の共通テストを実行してください。

```text
venv\Scripts\python.exe -m unittest discover -s tests
```

**元の83件に加えて新しく追加したテストも全部パスすること**を確認してください。

## 8. 守ってほしいこと

- 着手前に必ず `git fetch origin` と `git rebase origin/main` を実行する
- 企業提供画像・DBファイル（`manager.db`）・出力CSVをコミットしない。`git add -A` は使わず、変更したファイルを個別に指定して add する
- `print()` に絵文字を使わない（Windowsのcp932でUnicodeEncodeErrorになる）
- 分からないことがあれば本人（大久保）に聞く
- 今回は仕様表の転記だけです。設計判断を増やしたり、仕様にない項目を追加したりしないでください

## 9. push前の確認

```text
git status
venv\Scripts\python.exe -m unittest discover -s tests
```

`manager/record.py` など今回変更したファイルだけを個別に `git add` してください。`git status` に企業提供画像・`manager.db`・出力CSVが無いこと、元の83件に加えて新しく追加したテストも全部パスすることを確認してからpushしてください。

## 10. 最後に必ずやること：日報を書く

今日の作業が終わったら（途中で終わった場合も含めて）、日報をMarkdownファイル1枚にまとめてください。
ファイル名は `日報_<名前>_<YYYYMMDD>.md` です。書く内容は次の8項目です。
**分からない項目は「なし」と書き、空欄にしないでください。**

1. **名前と日付**
2. **今日のタスク名**（タスクIDを付ける。例: SK-02 Android雛形を実機で起動する）
3. **やったこと** — 実際に手を動かした内容を3〜5行で。やろうとしたが着手できなかったことは、そう書く
4. **成果物** — 作成・変更したファイルのパス。コミットしたなら**コミットのハッシュ（先頭7文字）と
   ブランチ名**も書く。コミットしていないなら「未コミット」と書く
5. **テスト結果** — 実行したコマンドと、何件中何件パスしたか。実行していないなら「未実行」と、その理由
6. **受け入れ条件の達成状況** — 渡された受け入れ条件を1つずつ書き出し、達成／未達成を付ける
7. **詰まったところ・分からなかったこと** — **ここが一番大事です。** 解決できなかった点、判断に迷った点、
   推測で進めた箇所を正直に書いてください。「特になし」で済ませず、少しでも自信のない箇所は書き出してください
8. **次に必要なこと** — 次回やること、または大久保に判断・確認してほしいこと

### 日報を書くときの注意

- **できなかったことを隠さないでください。** 進捗が無かった日も、どこで止まったかが分かれば次の手を打てます。
  うまくいったように書かれるのが一番困ります
- **推測で断定しないでください。** 確認していないことは「未確認」と書いてください
- 企業提供画像そのものや、その内容が分かる記述を日報に含めないでください
- 絵文字は使わないでください（Windowsのcp932で文字化けします）

書けたら、Slackチャンネル `#33-iot-ai-team-d` へファイル（.md または .txt）として投稿してください。
