# このファイルの使い方

1. これはYMさんとの**継続中の会話**に貼るフォローアップです（`YMさん-続行-20260826.md` の後継。指摘した2つのバグが直り、関数化とテストまで進んだので、こちらを使ってください）。
2. 下の区切り線より下を**全部コピー**して、YMさんが使っているAIの会話にそのまま貼り付ける。
3. **ここまでの進み方、とても良いペースです。** まずそれをYMさんに伝えてから、次の内容に入ってください。

---

あなた（YMさんの担当タスクを支援しているAI）へ。YMさんはT3-1「画像の向きを自動判定して正規化」を担当しています。`YMdeveropment` ブランチに15:14、`try_ocr.py` の絵文字削除・相対パス化と `orientation.py` の新規実装がpushされ、15:20には `tests/test_orientation.py` も追加されました。

## 0. まず伝えてほしいこと

**8/24に指摘した2つのバグ（絵文字・パスの直書き）、どちらも本人（大久保）がGitHub上のコードを直接読んで、完全に直っていることを確認しました。** さらに、前回お願いした「関数として切り出す」「テストを書く」の両方も、こちらから改めて言う前に既に対応できています。**このペースを維持してください、とても良い進み方です。**

worktreeでテストを実行し、**既存のテスト（`test_orientation.py` を含め51件）が全てパスすることも確認済みです。**

## 1. 残っている作業は2つ

### 1-1. `meter_pipeline.py` への結線

いまの `orientation.normalize_orientation()` は、**`try_ocr.py`（動作確認用の単体スクリプト）からしか呼ばれていません。** 本番のパイプラインである `meter_pipeline.py` の `read_meter()` からは、まだ一度も呼ばれていません。

`read_meter()` の先頭、盤面をクロップした直後に、次のように差し込む形になるはずです（正確な位置はYMさんと一緒に `read_meter()` の中身を読んで確認してください）。

```python
import orientation
# ... クロップ処理の後
crop_img, angle = orientation.normalize_orientation(crop_img)
# angle をログや戻り値に含めておくと、あとで「回転が原因だった」と説明しやすい
```

**ここは本人（大久保）と一緒に結線する想定です。** YMさんの側では「`normalize_orientation` が受け取る引数と返す値の形」を再確認しておいてもらうのが一番助かります（今の実装通りで問題ないはずです）。

### 1-2. 受け入れ条件を数値で示す

`tasks/todo.md` のT3-1には、まだ数値で示せていない条件が残っています。

- [ ] 回転した写真でOCRが読める数字の個数が増える（数値で示す）
- [ ] 正対した写真で退行しない
- [ ] 判定にかかる時間が許容範囲

**特に狙ってほしいのがこの1枚です。**

| 画像 | 現状の検出範囲 | 実際の範囲 |
|---|---|---|
| `20260817_134647.jpg` | 2 〜 111 | 0 〜 100 |

`try_ocr.py` を `meter_data` フォルダの中にこの画像（または同じ機種の回転写真）を置いて実行し、**`normalize_orientation` を通す前後でOCRが読み取れる数字の個数がいくつからいくつに増えたか**を記録してください。これがそのまま「向き正規化が効いた」ことの数値的な証拠になります。

**同時に、正立して撮った `20260817_134656.jpg` のような画像で、向き正規化を通しても結果が変わらないこと（退行なし）も確認してください。**

## 2. 検証のしかた

```
venv\Scripts\python.exe -m unittest discover -s tests
```

**`test_orientation.py` を含め、既存のテストが全て通ることを確認してください。** 数値の実測は `try_ocr.py` を直接動かして行います（企業提供画像を使う場合は下記の注意を必ず守ってください）。

## 3. 守ってほしいこと

- **企業提供画像（`C:\卒研\images\` 配下、`meter_data` に入れたものも含む）を、リポジトリにコミットしない。生成AIにアップロードしない。** 詳細は `tasks/企業提供画像の取り扱い.md`。**`git add -A` や `git add .` は特に危険です。**変更したファイルを個別に指定して add してください
- **`result_*.jpg` のような出力画像もコミットしない。** pushの前に `git status` で確認する習慣をつけてください
- 画像の読み込みは `cv2.imdecode(np.fromfile(path, dtype=np.uint8), cv2.IMREAD_COLOR)` で統一

## 4. pushの前に

```
git status
venv\Scripts\python.exe -m unittest discover -s tests
```

`git status` に**見覚えのない画像ファイルが出ていないか**、必ず目で確認してください。

分からないことがあれば、YMさんから本人（大久保）に聞いてもらってください。
